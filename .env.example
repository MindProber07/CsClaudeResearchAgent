# Copy this file to `.env` for local development (the .env file itself is git-ignored).
# The application reads ANTHROPIC_API_KEY as a real environment variable — the optional
# .env loader in this project only shims that in for local convenience. Never commit
# an actual key. Get a key from https://console.anthropic.com/settings/keys.
ANTHROPIC_API_KEY=
